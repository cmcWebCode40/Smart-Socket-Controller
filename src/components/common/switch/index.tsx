import {View} from 'react-native';
import React from 'react';
import {Typography} from '../typography';

export const SwitchIcon: React.FunctionComponent = () => {
  return (
    <View>
      <Typography>SwitchIcon</Typography>
    </View>
  );
};
